## Copy this file to `.env` and fill in values as needed

# MongoDB connection (used by pydantic settings in app/core/config.py)
# Example local instance:
MONGO_URI=mongodb://localhost:27017
MONGO_DB=railwayDB

# Optional: override server port when running `python -m app.main`
# PORT=8010
